# Retry failed requests up to 5 times (fixes ECONNRESET on flaky connections)
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Increase network timeout for large packages like hardhat-toolbox
fetch-timeout=120000
